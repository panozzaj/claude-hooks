#!/bin/bash
# Snapshot jj repo if we're in one
# Used by SessionStart and PreCompact hooks to capture state at critical moments

# Check if we're in a jj repo
if [[ -d .jj ]] || jj root >/dev/null 2>&1; then
  jj status >/dev/null 2>&1
fi
